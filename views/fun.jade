extends index2
block content
    div(id="options")
        button(id="moreSelec") Add selector on result
        br
        input(id="selector_1",placeholder="   selector 1 here") 
    div(id="htmlContainer")
        h5 
            =toSend.body
    div(id="shareLink")
        button Generate Link
        a(id="scrapeLink")
